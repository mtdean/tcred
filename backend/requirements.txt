# situation-monitor backend dependencies
# Versions pinned exactly to what resolves cleanly on Python 3.14.

# Web framework
fastapi==0.136.3
uvicorn[standard]==0.48.0

# Data fetching
aiohttp==3.13.5
feedparser==6.0.12
requests==2.34.2
yfinance==1.4.0
fredapi==0.5.2

# Scheduling
apscheduler==3.11.2

# Database
# (sqlite3 is stdlib — no install needed)

# Excel ingest (NY Fed Household Debt & Credit workbook)
openpyxl==3.1.5

# HTML table parsing (ABS pricing term sheets via pandas.read_html)
lxml==6.1.1

# HTML soup parsing (424B5 prospectus extractor in data/abs_parser.py)
beautifulsoup4==4.13.5

# Config
pyyaml==6.0.3
python-dotenv==1.2.2

# Claude API
anthropic==0.104.1

# Utils
numpy==2.4.6
pandas==3.0.3
