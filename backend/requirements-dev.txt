# situation-monitor backend dev/test dependencies
# Install on top of requirements.txt: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
responses==0.25.6        # mock for requests
aioresponses==0.7.7      # mock for aiohttp
httpx==0.28.1            # FastAPI TestClient transport
